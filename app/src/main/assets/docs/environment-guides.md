# 环境指南 (Environment Guides)

本目录是 **App 使用手册**，按功能模块说明界面怎么用，随 App 界面同步维护。

远程仓库（`github.com/Lisir2002/deepcode-R`）源码的 `docs/guides/` 下另有**通用技术指南**，偏向环境配置与工具链搭建，与具体界面无关。遇到环境类问题（容器编译、镜像导入、SDK/JDK 搭建等）时，可访问远端仓库 `docs/guides/` 目录获取参考（可用 GitHub API 列出该目录下的指南文件）。