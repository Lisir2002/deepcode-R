fix(terminal): TerminalUISpec 补上丢失的 androidx.compose.ui.unit.sp import

rc55/rc56 连续两次 CI release 构建失败的根因：整理 import 时
不小心删掉了 `import androidx.compose.ui.unit.sp`，但
TerminalLayout.DefaultFontSp = 14.sp 仍然在用，Kotlin 编译直接
Unresolved reference: sp，Step 11 Build release APK 失败。
