fix(terminal): 首屏提示 Banner 与终端内容同底色，不再用外壳 primaryContainer 造成深蓝卡片白边断层

用户指出截图里「AI 代码运行需要 Python」Banner 周围有白边，且 Banner
本身是深蓝色，看起来像外壳 UI 乱插到终端内容区里。根因是
TerminalFirstRunBanner 的 Card.containerColor =
MaterialTheme.colorScheme.primaryContainer（跟随程序外壳主题），
而它的父容器 Column 背景是 palette.containerBg（终端底色，可能是
用户在终端设置里选的白底黑字），两边拼在一起就是你看到的
「Banner 深蓝 + 卡片四周露出终端白底 8dp 的白边」。

修复：
1. Card.containerColor = palette.containerBg（与终端内容完全融为一体）
2. 文字颜色用 palette.defaultForeground（黑底白字/白底黑字都正确）
3. 提示层级靠「accentColor*0.08 标题软底 + accentColor*0.55 1dp 描边」
   来区分，对比度仍 ≥ 4.5:1（暗主题 secondary=7DD3FC / 亮主题 0284C7）
