fix(terminal): Banner/Tab 以上整块一律跟随程序主题（不再受终端白底黑字设置影响）

用户批评设计思路：「提示框那里的不管是提示框还是周围区域，应该跟随程序
主题懂不」——之前我把 Banner + TabBar + Terminal 全部塞进同一个
palette.containerBg Column，导致终端选「白底黑字」时，程序是暗主题的
情况下，Banner 周围 8dp padding 竟然透出终端白底色（截图里的断层）。

按用户规则重排 Layout 分层：
1) Tab 以上（Banner + 周围 padding + 空白间距）→ 放在 Scaffold.surface
   里，100% 跟随程序主题（通过 MaterialTheme.colorScheme）；
   Card.containerColor = primaryContainer / onPrimaryContainer，
   周围 padding 露出的就是 Scaffold surface（程序暗就是深蓝、程序亮就是浅），
   与终端选择彻底解耦。
2) Tab 及以下（TabBar / 1dp 分隔线 / TerminalSurface 内容区 / 搜索浮层 /
   Ctrl 气泡）→ 仍然包在 palette.containerBg Column 里，保证 Tab↔Terminal
   之间无白条；TabBar/ExtraKeys/分隔线 本就走 skin + MaterialTheme，
   「必然跟随程序」；仅 TerminalSurface 的内部颜色（fg/bg/cursor/256）
   才会受终端配色三档（跟随程序/黑底白字/白底黑字）影响。
